# Native library

Place the platform `libcavs_sdk` artifacts here under `<Platform>/` (Win64,
Mac, Linux) — see the plugin README. Downloaded from the `cavs-sdk-native-*`
release assets; not checked in.

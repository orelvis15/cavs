# Native libraries

Place the platform `libcavs_sdk` artifacts here (see the plugin README for the
per-platform layout). They are not checked in; download them from the
`cavs-sdk-native-*` release assets.
